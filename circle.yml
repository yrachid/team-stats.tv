machine:
  node:
    version: 7.8.0
dependencies:
  override:
    - npm install
    - npm install -g codeclimate-test-reporter
test:
  post:
    - CODECLIMATE_REPO_TOKEN=$CODECLIMATE_KEY codeclimate-test-reporter
