{
  "name": "tele-papa-bravo",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "build": {
    "appId": "io.github.othman853.telestats"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pack": "build --dir",
    "dist": "build",
    "start": "./node_modules/.bin/electron ./app/main.js --local-config=./stats.json --remote-host-name=localhost --remote-host-port=4567 --remote=localhost:4567"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^1.6.2"
  },
  "dependencies": {
    "query-string": "^4.3.4",
    "socket.io-client": "^1.7.3",
    "yargs": "^8.0.1"
  }
}
